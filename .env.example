# =============================================================================
# SpriteForge Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values.
# .env is gitignored — never commit secrets.

# ---------------------------------------------------------------------------
# Chat/gate endpoint (used by AzureChatProvider with DefaultAzureCredential).
# Prefer Foundry project endpoint; Azure OpenAI endpoint is also supported.
# ---------------------------------------------------------------------------
AZURE_AI_PROJECT_ENDPOINT=https://<your-foundry-project>.azure.com/
AZURE_OPENAI_ENDPOINT=https://<your-resource>.openai.azure.com/

# ---------------------------------------------------------------------------
# GPT-Image-1.5 — Entra ID authentication (DefaultAzureCredential)
# Only the base resource endpoint is needed; auth uses bearer tokens.
# No API key required.
# ---------------------------------------------------------------------------
AZURE_OPENAI_GPT_IMAGE_ENDPOINT=https://<your-resource>.openai.azure.com/

# ---------------------------------------------------------------------------
# Integration tests (set to 1 to enable)
# ---------------------------------------------------------------------------
SPRITEFORGE_RUN_INTEGRATION=0
